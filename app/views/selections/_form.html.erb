<%= form_for([@image, @selection]) do |f| %>
  <% if @selection.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@selection.errors.count, "error") %> prohibited this selection from being saved:</h2>

      <ul>
      <% @selection.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :start_x %><br>
    <%= f.number_field :start_x %>
  </div>
  <div class="field">
    <%= f.label :start_y %><br>
    <%= f.number_field :start_y %>
  </div>
  <div class="field">
    <%= f.label :end_x %><br>
    <%= f.number_field :end_x %>
  </div>
  <div class="field">
    <%= f.label :end_y %><br>
    <%= f.number_field :end_y %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
