<p id="notice"><%= notice %></p>

<div>
    <p><%= @image.url %></p>
  <%= image_tag(@image.url, class: (@image.reviewed ? "img_reviewed" : "img_unreviewed") ) %>
</div>

<p>Using your mouse, make a selection</p>
<%= link_to 'Back', images_path %>
