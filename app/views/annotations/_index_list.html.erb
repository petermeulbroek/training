<table class="table table-striped table-bordered table-condensed" id="annotations-list">
  <thead>
    <tr>
        <th>Text</th>
        <th>X</th>
        <th>Y</th>
        <th>Width</th>
        <th>Height</th>
        <td colspan="1">Actions</td>
    </tr>
  </thead>

  <tbody id="annotations-list-body">
    <% annotations.each do |annotation| %>
      <tr id=<%= annotation.id %> >
          <td><%= annotation.text %></td>
          <td><%=  annotation.shapes.first.x %></td>
          <td><%=  annotation.shapes.first.y %></td>
          <td><%=  annotation.shapes.first.width %></td>
          <td><%=  annotation.shapes.first.height %></td>
        <td><%= link_to 'Edit', edit_annotation_path(annotation) %></td>
      </tr>
    <% end %>
  </tbody>
</table>
